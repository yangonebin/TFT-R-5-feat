

# 📈 추천 알고리즘 상세 설계 및 의사결정 보고서 (FinMatch)
주식가격_AI_예측_서비스_(TFT-R).pdf 를 꼭 보시는 걸 권장드립니다.
## 1. 개요 (Overview)

본 문서는 FinMatch 서비스의 핵심 기능인 **'AI 주가 예측 및 매수 시그널 추천'** 알고리즘의 설계 및 구현 과정을 기술한다. 초기 LSTM 기반의 실험부터 최종 SOTA(TFT) 모델 도입까지의 **가설 수립  검증  실패  피벗(Pivot)** 과정을 상세히 기록하며, 5-Feature 기반의 TFT Regressor 모델이 최종적으로 선정된 공학적 근거를 제시한다. 위 보고서는 주식가격_AI_예측_서비스_(TFT-R).pdf의 요약본이다.

---
## 2. 알고리즘 발전 및 의사결정 과정 (Evolution & Decision Process)

본 프로젝트는 총 5단계의 모델 고도화 과정을 거쳐, 기술적 지표(RMSE)가 아닌 **비즈니스 가치(ROI)**를 극대화하는 방향으로 의사결정을 수행했다.

### Phase 1: 가설 수립 및 베이스라인 (Regression vs Classification)

* **초기 접근:** 주가 예측을 '수치 예측(Regression)'으로 볼 것인가, '방향성 예측(Classification)'으로 볼 것인가에 대한 A/B 테스트 설계.


* **Baseline (Regression):** MSE 손실 함수 기반 LSTM. 주가의 흐름을 추종.
* **Challenger (Classification):** Binary Crossentropy 기반 LSTM. 상승/하락 확률 예측.
* **초기 결과:** 4배 패널티(4x Penalty) 적용 시 리스크 회피 성향으로 인해 '거래 마비(Trading Paralysis, 수익률 0%)' 현상 발생.


* **의사결정:** 패널티를 2배로 완화하고, 데이터 전처리 방식을 개선하여 재실험 진행.



### Phase 2: 기술적 문제 해결 (Beast V2)

* **문제점:** Classification 모델 학습 시 Gradient Vanishing 및 0.5 확률 수렴 현상 발생 (판단 유보).


* **해결책 (Beast V2):*** 
**Batch Normalization:** 각 레이어 직후 배치 정규화를 도입하여 기울기 소실 방지.


* **He Initialization & ReLU:** 가중치 초기화 전략 변경으로 초기 학습 속도 및 수렴성 개선.




* **성과:** 평균 ROI 62.28% 달성, T-test 검정 결과 회귀 모델 대비 통계적 우위 입증.



### Phase 3: 자산 방어 전략 (Beast V3 Vault)

* **문제점:** V2 모델의 높은 수익률(Max 135%) 대비 높은 변동성(SD 34.71) 리스크 확인.


* **전략 (금고 전략):** 수익 발생 시 즉시 별도 계좌(Vault)로 격리하고, 손실 시 복구하는 자금 관리 로직 추가.


* **의사결정:** 안정성은 높아졌으나 평균 ROI가 45.21%로 하락하여 수익 잠재력이 훼손됨. 전략적 폐기 후 V2 모델 유지 결정.



### Phase 4: 터닝 포인트 (The Turning Point) - Feature Selection

* **발견:** 코드 실수로 거시경제 지표(환율, 금리)와 일목균형표 등 9개 피처가 누락된 **5-Feature(OHLCV)** 회귀 모델이 실행됨.


* **충격적 결과:** 기존의 정교한 14-Feature 분류 모델보다, 단순한 5-Feature 회귀 모델이 **ROI 112.52%**라는 압도적 성과를 기록.


* **원인 분석:** 복잡한 파생 변수(일목균형표 등)가 딥러닝 모델에게는 오히려 학습을 방해하는 **노이즈(Noise)**로 작용했음을 확인 .


* **최종 의사결정:** "데이터는 거짓말하지 않는다"는 원칙 하에, 기존 가설(분류 우월, 다변량 피처 우월)을 전면 폐기하고 **5-Feature 기반으로 회귀(Pivot)** 결정.



### Phase 5: 최종 모델 선정 (SOTA: TFT Regressor)

* **고도화:** LSTM의 한계(Long-term dependency)를 극복하기 위해 어텐션 메커니즘이 적용된 **TFT (Temporal Fusion Transformer)** 도입.


* **최종 검증:** TFT Regressor (5-Feat) 모델이 **평균 ROI 103.68%, 최고 ROI 292.45%**를 기록하며 최종 모델로 선정.



---

## 3. 최종 알고리즘 상세 설계 (TFT Regressor 5-Feat)

### 3.1 모델 아키텍처 (Model Architecture)

최종 선정된 모델은 **Temporal Fusion Transformer (TFT)** 구조를 기반으로 하며, 시계열 데이터의 장기 의존성과 중요 변곡점을 포착하도록 설계되었다.

1. **Input Layer:** (Batch Size, 20, 5) 형태의 3D 텐서 입력.


* Look-back Window: 20일 (약 1달 거래일).


* Features (5개): Log Returns of Open, High, Low, Close, Volume.




2. **VSN (Variable Selection Network):** 입력된 5개 피처 중 현재 시점에서 가장 중요한 변수에 가중치를 부여하여 선택적 학습 수행.


3. **Temporal Context (LSTM):** `Stacked LSTM`을 통해 시계열의 순차적 흐름과 맥락(Context)을 인코딩.


4. **Multi-Head Attention:** 과거 20일 중 미래 가격 변동에 결정적 영향을 미치는 특정 시점(Time Step)에 집중(Attention).


5. **GRN (Gated Residual Network):** 불필요한 비선형 노이즈를 필터링하고 정보의 통과량을 제어.


6. **Output Layer:** `Dense(1)` 레이어를 통해 익일의 **단순 수익률(Simple Return)** 예측값 출력.



### 3.2 데이터 파이프라인 (Data Pipeline)

1. **수집 (Ingestion):** Yahoo Finance API를 통해 OHLCV 원천 데이터 수집.


2. **전처리 (Preprocessing):*** 
**로그 수익률 변환:** 가격의 비정상성(Non-Stationarity) 제거를 위해  변환 적용.


* **스케일링:** `StandardScaler`를 사용하여 평균 0, 표준편차 1로 정규화 (MinMaxScaler의 한계 극복).




3. **시퀀스 생성:** Sliding Window 방식을 사용하여  형태의 학습 데이터셋 구성.



### 3.3 학습 및 최적화 전략 (Training Strategy)

* **Loss Function:** MSE (Mean Squared Error) - 오차의 제곱을 최소화하여 대형 손실 방지.


* **Optimizer:** Adam (Learning Rate: 1e-4).


* **Early Stopping:** Validation Loss 기준 15회 개선 없을 시 조기 종료 및 Best Weight 복원.


* **Reproducibility:** Random Seed(22) 고정 및 GPU Deterministic 옵션 활성화를 통해 재현성 확보.


---

## 4. 서빙 및 추천 전략 (Serving & Recommendation)

### 4.1 추천 로직 (Decision Rule)

모델이 예측한 값은 **익일의 예상 수익률**이며, 이를 기반으로 다음과 같이 추천 시그널을 생성한다.

* **BUY (매수):** `Predicted_Return > 0` (양의 수익률이 예상될 때).


* **SELL (매도):** `Predicted_Return <= 0` (하락 또는 보합이 예상될 때).



### 4.2 시간외 단일가 매매 전략 (After-Hours Strategy)

장중 변동성이나 갭상승(Gap-Up) 리스크를 회피하기 위해, 데이터가 확정되는 시점에 즉시 추천을 수행한다.

1. **15:30:** 정규장 마감 및 당일 OHLCV 데이터 확정.


2. **15:40:** FastAPI 서버에서 실시간 데이터 수집 및 추론 수행.


3. **16:00 ~ 18:00:** 'BUY' 시그널 발생 시, **시간외 단일가**로 선취매 수행.



---

## 5. 회고

이 파트는 주식가격_AI_예측_서비스_(TFT-R).pdf의 [19. 프로젝트 회고: 데이터 사이언티스트의 양심과 성장] 부분을 발췌하였다.
---

### 19-1. 오만과 실패: A/B 테스트의 높은 벽

프로젝트 초기, 저는 A/B 테스트를 단순히 "두 모델을 만들어서 결과만 비교하면 되는 쉬운 과정"이라고 생각했습니다. "그냥 회귀랑 분류 만들어서 수익률 더 높은 거 고르면 끝 아니야?"라는 생각은 저의 **오만**이었습니다.

실험이 진행될수록 **엄밀하게 통제된 A/B 테스트**가 얼마나 어려운지 깨닫게 되었습니다. 서로 다른 손실 함수(Loss Function)를 가진 모델을 동일선상에서 비교하려는 시도 자체가 논리적 모순이었으며, 변인을 완벽하게 통제하지 못한 실험 설계는 결국 **실패**로 돌아갔습니다.

이 과정을 통해 "모델링보다 중요한 것은 실험 설계이며, 첫 단추를 잘못 끼우면 아무리 좋은 코드를 짜도 결론에 도달할 수 없다"는 것을 배웠습니다. 엄밀한 의미에서의 A/B 테스트는 실패했지만, 그 실패 덕분에 실험 설계의 중요성을 몸으로 익힐 수 있었습니다.


### 19-2. 우연한 발견과심: "이걸 못 본 척할까?"

그렇게 A/B 테스트의 실패를 수습하며 "분류 모델이 그나마 낫다"는 결론으로 프로젝트를 마무리하려던 찰나, 우연한 실수가 발생했습니다. 코드 오류로 피처 9개가 누락된 채 돌아간 엉성한 회귀 모델이, 공들여 만든 분류 모델보다 훨씬 높은 **123%의 수익률**을 기록한 것입니다.

이 순간, 저는 데이터 사이언티스트로서 거대한 시험에 들었습니다.

이미 "분류 모델이 우월하다"는 논리로 13개 챕터의 보고서를 다 써놓은 상태였습니다. 마감은 코앞이었고, 이 우연한 결과를 정말 '우연'로 치부하고 덮어버리면 저는 편안하게 프로젝트를 끝낼 수 있었습니다.

**하지만, "데이터를 조작하지는 않더라도, 불편한 데이터를 외면하는 것도 거짓말 아닐까?"**

이 질문 앞에서 저는 고민했고, 결국 양심을 선택했습니다.

### 19-3. 진정한 성장: 나의 실패를 직시하는 용기

저는 기존에 세웠던 "분류가 우월하다"는 가설이 틀렸음을 인정했습니다. 대신, "왜 5개 피처가 더 잘 작동하는가?"라는 새로운 질문을 던지며 데이터가 가리키는 진실(5-Feature)을 따라 처음부터 다시 검증했습니다.

저의 진정한 성장은 코딩 실력이 늘거나 SOTA 모델을 구현한 것에 있지 않습니다.

나의 가설이 틀렸음을 인정할 수 있는 용기, 우연히 발견한 진실을 외면하지 않는 정직함, 그리고 실패를 딛고 다시 원점으로 돌아갈 수 있는 끈기.

이 프로젝트를 통해 저는 단순히 파이썬을 다루는 코더에서, 데이터 앞에서 겸손하고 진실된 태도를 갖춘 **진짜 엔지니어**로 한 단계 성장했습니다. FinMatch 프로젝트는 저에게 '기술'이 아니라 '태도'를 가르쳐준 인생의 프로젝트였습니다.

---

> "가장 화려한 모델보다 가장 정직한 데이터가 강력하다는 것을,
> 
> 
> 나의 실패와 양심을 걸고 증명한 프로젝트."
> 

---